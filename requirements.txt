langgraph
langchain
langchain-ollama
langchain-core
python-dotenv
pymupdf
pillow
pytesseract

# Alternatives — install whichever you prefer instead of Ollama:
# langchain-openai          (needs OPENAI_API_KEY in .env)
# langchain-google-genai    (needs GOOGLE_API_KEY in .env)
